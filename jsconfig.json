{
  "compilerOptions": {
    "baseUrl": "./src",
    "jsx": "react",
    "paths": {
      "@Components": ["components/index"],
      "@Styles": ["styles/index"],
      "@Screens": ["screens/index"],
      "@State": ["state/index"] // Do not use "redux" to prevent conflicts.
    }
  }
}
